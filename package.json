{
  "name": "next-asity",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "start http://localhost:3000/ && next dev",
    "build": "next build",
    "start": "start http://localhost:3000/ && next start",
    "lint": "next lint",
    "serve-json": "start http://localhost:4000/ && json-server --watch db.json --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "next": "12.0.7",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "swr": "^1.2.2"
  },
  "devDependencies": {
    "eslint": "8.4.0",
    "eslint-config-next": "12.0.7"
  }
}
